const show = true;
const showProgress = true;
const title = "Awesome Alert";
const message= "I am just a message";
const closeOnTouchOutside = true;
const closeOnHardwareBackPress = true;
const showCancelButton = true;
const showConfirmButton = true;
const cancelText = "Dismiss";
const confirmText = "Confirm";
const cancelButtonColor = "#D0D0D0";
const confirmButtonColor = "#AEDEF4";

const props = {
  show,
  showProgress,
  title,
  message,
  closeOnTouchOutside,
  closeOnHardwareBackPress,
  showCancelButton,
  showConfirmButton,
  cancelText,
  confirmText,
  cancelButtonColor,
  confirmButtonColor
};

export default props;
